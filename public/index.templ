package public

templ Index() {
@DocType()
@Head()
@Body(pageIndex())
}

templ pageIndex() {
<div class="flex flex-col">
    <p>Hello Anon User!</p>
    <p>Don't be afraid to add the spiciest messages.</p>

    <div id="create-room">
        <button hx-get="/create/room" hx-target="#create-room" hx-swap="innerHTML"
            class="px-3 py-1 bg-zinc-800 text-white flex items-center justify-center">Create A room!</button>
    </div>
</div>
}
